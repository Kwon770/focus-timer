import React from "react";
import styled from "styled-components";

export default function TodaySetCounter(props) {
  return <SetContainer></SetContainer>;
}

const SetContainer = styled.ul`
  position: absolute;
`;

const Set = styled.li``;
